{
  "name": "@ttrpg/server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "pytest tests/ -x -q",
    "test:run": "pytest tests/ -q",
    "test:coverage": "pytest tests/ --cov=. --cov-report=term -q",
    "lint": "ruff check ."
  }
}
