{
  "name": "@ttrpg/core-table",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "pytest -x -q",
    "test:run": "pytest -q",
    "lint": "ruff check .",
    "bench": "pytest tests/bench_*.py --benchmark-only --benchmark-sort=mean --benchmark-columns=mean,stddev,rounds"
  }
}
